SQLite files for local prototyping.

- Database file created by script: `email_marketing.sqlite`
- Seed command:
  - `npm run seed:email-marketing-sqlite`

The `.sqlite` database files are intentionally gitignored.
