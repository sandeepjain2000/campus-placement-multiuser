import '@/components/developer/developer-notes.css';

/** Shared Developer Notes chrome (TOC, sections, schema article). */
export default function DeveloperLayout({ children }) {
  return children;
}
